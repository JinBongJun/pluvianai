                                        <div className="p-6">
                                            <div className="flex flex-col gap-8">
                                                {/* Main 2-column Layout */}
                                                <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
                                                    
                                                    {/* Left Column: Core Data (Prompts, Output) */}
                                                    <div className="lg:col-span-2 space-y-8">
                                                        {/* Headers / Meta */}
                                                        <div className="flex items-center justify-between border-b border-white/5 pb-4">
                                                            <div className="flex items-center gap-6">
                                                                <div className="flex flex-col">
                                                                    <span className="text-[11px] text-slate-500 font-mono">Snapshot ID</span>
                                                                    <span className="text-xs text-slate-200 font-mono tracking-wide">{s.id}</span>
                                                                </div>
                                                                <div className="h-8 w-px bg-white/10" />
                                                                <div className="flex flex-col">
                                                                    <span className="text-[11px] text-slate-500 font-mono">Trace ID</span>
                                                                    <span className="text-xs text-slate-200 font-mono tracking-wide">{s.trace_id || '-'}</span>
                                                                </div>
                                                                <div className="h-8 w-px bg-white/10" />
                                                                <div className="flex flex-col">
                                                                    <span className="text-[11px] text-slate-500 font-mono">Model</span>
                                                                    <span className="text-xs text-slate-200">{s.agent_id || '-'} / {normalizeModelVersion(s)}</span>
                                                                </div>
                                                            </div>
                                                            <div className="flex items-center gap-4">
                                                                <span className={clsx(
                                                                    "px-3 py-1 text-[11px] font-black uppercase tracking-wide rounded border",
                                                                    successLike ? "border-emerald-500/30 text-emerald-400 bg-emerald-500/10" : "border-rose-500/30 text-rose-400 bg-rose-500/10"
                                                                )}>
                                                                    {successLike ? 'Success' : 'Fail'}
                                                                </span>
                                                                <span className="text-xs font-mono text-slate-400">{s.latency_ms ?? '-'}ms</span>
                                                                <span className="text-xs font-mono text-slate-400">{s.status_code ?? '200 OK'}</span>
                                                            </div>
                                                        </div>

                                                        {/* Content Sections */}
                                                        <div className="space-y-6">
                                                            <div>
                                                                <span className="text-[11px] font-black text-slate-500 uppercase tracking-widest mb-3 block border-l-2 border-slate-600 pl-2">System Prompt</span>
                                                                <div className="text-xs text-slate-400 font-mono leading-relaxed bg-transparent p-1 max-h-[300px] overflow-y-auto whitespace-pre-wrap">
                                                                    {s.system_prompt || '-'}
                                                                </div>
                                                            </div>
                                                            <div>
                                                                <span className="text-[11px] font-black text-slate-500 uppercase tracking-widest mb-3 block border-l-2 border-slate-600 pl-2">User Input</span>
                                                                <div className="text-xs text-white font-mono leading-relaxed bg-transparent p-1 max-h-[300px] overflow-y-auto whitespace-pre-wrap">
                                                                    {s.request_prompt || s.user_message || '-'}
                                                                </div>
                                                            </div>
                                                            {customCode && (
                                                                <div>
                                                                    <span className="text-[11px] font-black text-slate-500 uppercase tracking-widest mb-3 block border-l-2 border-slate-600 pl-2">Custom Code</span>
                                                                    <div className="text-[11px] text-slate-500 font-mono leading-relaxed bg-black/30 p-4 rounded-lg border border-white/5 whitespace-pre-wrap max-h-[200px] overflow-y-auto">
                                                                        {customCode}
                                                                    </div>
                                                                </div>
                                                            )}
                                                            <div className="pt-2">
                                                                <span className="text-[11px] font-black text-emerald-500/70 uppercase tracking-widest mb-3 block border-l-2 border-emerald-500/50 pl-2">Agent Response</span>
                                                                <div className="text-xs text-emerald-100 font-mono leading-relaxed bg-transparent p-1 max-h-[500px] overflow-y-auto whitespace-pre-wrap">
                                                                    {s.response_text || s.response || '-'}
                                                                </div>
                                                            </div>
                                                        </div>
                                                    </div>

                                                    {/* Right Column: Technical Details & Sidebar */}
                                                    <div className="space-y-8 lg:pl-8 lg:border-l lg:border-white/5">
                                                        
                                                        {/* Meta details list */}
                                                        <div className="space-y-4">
                                                            <span className="text-[11px] font-black text-slate-500 uppercase tracking-widest block">Execution Details</span>
                                                            <ul className="space-y-1 text-xs font-mono">
                                                                <li className="flex justify-between items-center py-2 border-b border-white/5 border-dashed">
                                                                    <span className="text-slate-500">Timestamp</span>
                                                                    <span className="text-slate-300">{fullTime}</span>
                                                                </li>
                                                                <li className="flex justify-between items-center py-2 border-b border-white/5 border-dashed">
                                                                    <span className="text-slate-500">Tokens Used</span>
                                                                    <span className="text-slate-300">{s.tokens_used ?? '-'}</span>
                                                                </li>
                                                                <li className="flex justify-between items-center py-2 border-b border-white/5 border-dashed">
                                                                    <span className="text-slate-500">Estimated Cost</span>
                                                                    <span className="text-slate-300">{s.cost ?? '-'}</span>
                                                                </li>
                                                            </ul>
                                                        </div>

                                                        {/* Step Logs */}
                                                        <div className="space-y-4">
                                                            <span className="text-[11px] font-black text-slate-500 uppercase tracking-widest block">Execution Steps</span>
                                                            <div className="space-y-1">
                                                                {!stepLogs.length && <p className="text-xs text-slate-500 font-mono italic px-2">No steps recorded</p>}
                                                                {stepLogs.map((step, idx) => (
                                                                    <div key={`${s.id}-step-${idx}`} className="flex flex-col py-2 border-b border-white/5 border-dashed">
                                                                        <div className="flex items-center justify-between">
                                                                            <div className="flex items-center gap-2">
                                                                                <span className={clsx(
                                                                                    "w-1.5 h-1.5 rounded-full",
                                                                                    step.status.includes('pass') || step.status.includes('success') ? "bg-emerald-500" :
                                                                                    step.status.includes('fail') || step.status.includes('error') ? "bg-rose-500" : "bg-slate-500"
                                                                                )} />
                                                                                <span className="text-[11px] text-slate-300 font-mono truncate max-w-[140px]" title={step.name}>{step.name}</span>
                                                                            </div>
                                                                            <span className="text-[10px] text-slate-500 font-mono">{step.runtimeMs != null ? `${step.runtimeMs}ms` : ''}</span>
                                                                        </div>
                                                                        {step.detail && (
                                                                            <div className="text-[10px] text-slate-500 font-mono mt-1 pl-3.5 truncate">{step.detail}</div>
                                                                        )}
                                                                    </div>
                                                                ))}
                                                            </div>
                                                        </div>

                                                        {/* Eval & Policy Checks */}
                                                        <div className="space-y-4">
                                                            <span className="text-[11px] font-black text-slate-500 uppercase tracking-widest block">Checks & Validations</span>
                                                            <div className="space-y-1 bg-white/[0.01] rounded-lg border border-white/5 p-2">
                                                                {/* Policy Single Row */}
                                                                <div className="flex items-center justify-between px-2 py-1.5 rounded hover:bg-white/[0.02] transition">
                                                                    <div className="flex items-center gap-2">
                                                                        <ShieldCheck className="w-3.5 h-3.5 text-slate-400" />
                                                                        <span className="text-[11px] text-slate-300">Policy Validation</span>
                                                                    </div>
                                                                    <span className={clsx(
                                                                        "text-[10px] font-black uppercase tracking-wider",
                                                                        policyState.status === 'pass' ? "text-emerald-400" :
                                                                        policyState.status === 'fail' ? "text-rose-400" : "text-slate-500"
                                                                    )}>
                                                                        {policyState.status}
                                                                    </span>
                                                                </div>
                                                                
                                                                {/* Eval Breakdown rows */}
                                                                {evalEnabled && evalRows.length > 0 && evalRows.map((row) => (
                                                                    <div key={row.id} className="flex items-center justify-between px-2 py-1.5 rounded hover:bg-white/[0.02] transition">
                                                                        <div className="flex items-center gap-2">
                                                                            <Scale className="w-3.5 h-3.5 text-slate-400" />
                                                                            <span className="text-[11px] text-slate-300 truncate max-w-[140px]" title={EVAL_CHECK_LABELS[row.id]}>{EVAL_CHECK_LABELS[row.id] || row.id}</span>
                                                                        </div>
                                                                        <span className={clsx(
                                                                            "text-[10px] font-black uppercase tracking-wider",
                                                                            row.status === 'pass' ? "text-emerald-400" :
                                                                            row.status === 'fail' ? "text-rose-400" : "text-slate-500"
                                                                        )}>
                                                                            {row.status}
                                                                        </span>
                                                                    </div>
                                                                ))}
                                                                {evalEnabled && evalRows.length === 0 && (
                                                                    <p className="text-[10px] text-slate-500 italic px-2 py-1">No eval configured</p>
                                                                )}
                                                            </div>
                                                            <div className="pt-2">
                                                                <button
                                                                    onClick={() => runPolicyCheck(s)}
                                                                    disabled={policyState.status === 'loading' || !s.trace_id || !configuredPolicies.length || !hasPolicyContext}
                                                                    className="w-full py-1.5 rounded-md bg-white/[0.03] border border-white/5 text-[10px] font-black uppercase tracking-wide text-slate-300 hover:bg-white/[0.06] disabled:opacity-50 transition"
                                                                >
                                                                    {policyState.status === 'loading' ? 'Running...' : 'Re-run Validation'}
                                                                </button>
                                                            </div>
                                                        </div>

                                                        {/* Actions */}
                                                        <div className="pt-6 space-y-2">
                                                            <button
                                                                onClick={() => {
                                                                    if (!orgId || !projectId) return;
                                                                    const traceId = String(s.trace_id || '').trim();
                                                                    if (!traceId) return;
                                                                    const query = new URLSearchParams({
                                                                        trace_id: traceId,
                                                                        agent_id: String(s.agent_id || agentId || ''),
                                                                        snapshot_id: String(s.id || ''),
                                                                        source: 'live_view',
                                                                    });
                                                                    router.push(`/organizations/${orgId}/projects/${projectId}/release-gate?${query.toString()}`);
                                                                }}
                                                                className="w-full flex justify-center items-center gap-2 py-2 rounded-md bg-violet-500/10 border border-violet-500/20 text-[11px] font-black uppercase tracking-wide text-violet-300 hover:bg-violet-500/20 transition"
                                                                title="Open this run in the Release Gate laboratory for testing and debugging"
                                                            >
                                                                <Flag className="w-3.5 h-3.5" />
                                                                Fix in Laboratory
                                                            </button>
                                                            <div className="flex gap-2">
                                                                <button
                                                                    onClick={() => downloadSnapshotJSON(s)}
                                                                    className="flex-1 flex justify-center items-center gap-1.5 py-1.5 rounded-md border border-white/5 bg-white/[0.02] hover:bg-white/[0.05] text-[10px] font-black uppercase tracking-wide text-slate-400 transition"
                                                                >
                                                                    <Download className="w-3 h-3" /> Export
                                                                </button>
                                                                <button
                                                                    onClick={() => {
                                                                        const value = String(s.trace_id || s.id || '');
                                                                        if (value) navigator.clipboard?.writeText(value).catch(() => undefined);
                                                                    }}
                                                                    className="flex-1 flex justify-center items-center gap-1.5 py-1.5 rounded-md border border-white/5 bg-white/[0.02] hover:bg-white/[0.05] text-[10px] font-black uppercase tracking-wide text-slate-400 transition"
                                                                >
                                                                    <Copy className="w-3 h-3" /> Copy ID
                                                                </button>
                                                            </div>
                                                        </div>

                                                    </div>
                                                </div>
                                            </div>
                                        </div>
